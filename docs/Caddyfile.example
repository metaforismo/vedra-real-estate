# Example for a host-native Vedra process on 127.0.0.1:8000.
# Configure DNS first; replace preview.example.com with your own domain.
preview.example.com {
    request_body {
        max_size 5MB
    }
    @machine path /bridge/*
    respond @machine 404
    reverse_proxy 127.0.0.1:8000
}
